# 今日更新

## 1. 修复：Planner JSON 解析失败导致回退到 `single_agent`（无法执行复杂任务）

**文件**: `crates/workflow/src/stages.rs`

### 问题
- `max_tokens: 2000` 过小，当任务需要大量 sub_tasks（如 14 年的年度大事）时，LLM 输出被截断
- 截断的 JSON 导致 `EOF while parsing a string` 错误，回退到 `single_agent()`
- `strip_markdown_fences()` else 分支未 trim

### 修复
- `max_tokens` -> **4000**
- 新增 `fix_truncated_json()` 自动修复截断 JSON（关闭未闭合字符串/大括号/方括号）
- `strip_markdown_fences` else 分支改为返回 `trimmed.to_string()`

---

## 2. 新增：启动时搜索后端健康检测

**文件**: `crates/tool/src/health.rs`（新增 310 行）

### 功能
- 启动时对 Serper / Tavily / Bocha / PubMed 各发一条测试查询
- 记录不可用的后端到全局 `HealthState`
- 运行时 `web_search` 根据健康状态跳过不可用后端，避免无谓 HTTP 调用

### 修改的文件
| 文件 | 变更 |
|---|---|
| `crates/tool/src/lib.rs` | 导出 `health` 模块和 `probe_all_backends` |
| `crates/tool/src/tools/web_search.rs` | `execute()` 检查 `health_state().is_healthy(name)`，不可用跳过 |
| `crates/server/src/bin/miniagent-server.rs` | 启动时通过 `tokio::spawn` 异步执行健康检测，不阻塞 HTTP 服务 |
| `crates/cli/src/main.rs` | 搜索命令启动时通过 `tokio::spawn` 异步执行健康检测 |

### 测试
- 8 个单元测试，覆盖健康状态管理、全局读写、无效 key 的场景

---

## 3. 改造：同一对话窗口内累积多轮消息

### 问题
- 之前每次发送消息都创建新 `task`，每个任务只有一问一答
- 无法在同一对话内连续追问

### 修改的文件

**`crates/server/src/state.rs`**
- `TaskInfo` 新增 `messages: Vec<Value>` 字段存储多轮 `{role, content}`

**`crates/server/src/routes.rs`**
- `handle_ws`: `"run"` 传入 `task_id` 时作为后续消息，`get_task` 返回完整 `messages`
- `handle_run`: 接受 `existing_task_id`，复用已有 task，追加用户消息到 `messages`
- `finalize_task`: 完成时追加 assistant 消息到 `messages`
- 新增 `create_new_task()` 辅助函数

**`crates/server/src/lib.rs`**
- `restore_tasks_from_disk`: 恢复时重建 `messages`

**`crates/server/src/static/index.html`**
- `sendMessage()`: 包含 `task_id: currentTaskId`，后续消息进入同一对话
- `renderHistory(msg)`: 优先渲染 `msg.messages` 数组（多轮）

---

## 4. E2E 多循环管道测试（loop-pipeline）

**文件**: `crates/loop-pipeline/tests/integration_test.rs`

### 新增 4 个测试

| 测试 | 内容 |
|---|---|
| `test_e2e_multi_loop_self_evaluate_and_optimize` | 2 轮循环：Explore→Plan→Dispatch(1失败)→Evaluate→Repair→Explore→Plan→Dispatch(全成功)→Evaluate(100%停止) |
| `test_e2e_no_progress_safety_stops_infinite_loop` | 4 轮卡在 66%，第 4 轮触发安全刹车 |
| `test_e2e_max_loops_boundary_forced_stop` | max_loops=3 边界强制停止 |
| `test_e2e_cross_stage_message_routing` | 15 条消息验证完整跨阶段路由路径 |

全部 38 个离线测试通过（14 单元 + 24 集成），包括新增的 7 个 E2E 多循环测试。

### 新增高级多循环测试（第 2 轮）

| 测试 | 内容 |
|---|---|
| `test_multi_loop_dynamic_plan_evolution` | 3 轮循环，plan 结构跨轮次演进（4任务→拆分失败任务为2→成功），验证 repair 的 `requires_re_plan=true` 驱动 plan 重构 |
| `test_multi_loop_quality_self_assessment` | 评估器判断输出质量不足（即使全部任务"通过"），决定继续优化；对比轮次 1 的浅层输出和轮次 2 的深度输出 |
| `test_multi_loop_multiple_failure_modes` | 同一任务中两种不同类型的失败：工具错误（可重试）和逻辑错误（需重新规划），不同 repair 路由到不同下游阶段 |

---

## 构建

```bash
cargo build --release -p miniagent-server
# 输出: target/release/miniagent-server.exe
```
